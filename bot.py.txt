from telegram import Update
from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes

TOKEN = "8905216177:AAHWPVFxT5LH5s4pVL514vr4ZsC3BrwQmzg"

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "سلام! برایم ضرب یا تفریق بنویس:\n5*3\nیا\n10-4"
    )

async def calculate(update: Update, context: ContextTypes.DEFAULT_TYPE):
    text = update.message.text

    try:
        if "+" in text:
            a, b = text.split("*")
            result = float(a) + float(b)

        elif "*" in text:
            a, b = text.split("*")
            result = float(a) * float(b)

        else:
            await update.message.reply_text("فقط جمع و تفریق وارد کن.")
            return

        await update.message.reply_text(f"نتیجه: {result}")

    except:
        await update.message.reply_text("فرمت درست نیست.")

app = Application.builder().token(TOKEN).build()

app.add_handler(CommandHandler("start", start))
app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, calculate))

app.run_polling()